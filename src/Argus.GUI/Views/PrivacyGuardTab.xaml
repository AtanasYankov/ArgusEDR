<UserControl x:Class="Argus.GUI.Views.PrivacyGuardTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Privacy Guard" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding GroupedToggles}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{Binding Key}" FontWeight="SemiBold"
                                       FontSize="14" Margin="0,0,0,6"
                                       Foreground="#555"/>
                            <ItemsControl ItemsSource="{Binding}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#E0E0E0" BorderThickness="1"
                                                CornerRadius="4" Padding="12,8" Margin="0,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}"
                                                               FontWeight="Medium"/>
                                                    <TextBlock Text="{Binding Description}"
                                                               Foreground="#666" FontSize="11"
                                                               TextWrapping="Wrap"/>
                                                    <TextBlock
                                                        Text="{Binding WhatBreaks, StringFormat='Impact: {0}'}"
                                                        Foreground="#999" FontSize="10"
                                                        Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <ToggleButton Grid.Column="1"
                                                    IsChecked="{Binding Enabled}"
                                                    Width="50" Height="24"
                                                    Margin="12,0,0,0"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                       Margin="0,0,16,0" Foreground="#666"/>
            <Button Content="Apply Privacy Settings"
                    Command="{Binding ApplySelectedCommand}"
                    Padding="16,8" Background="#1976D2" Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>
