<UserControl x:Class="Argus.GUI.Views.DefenderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Argus.GUI.ViewModels">

    <ScrollViewer Style="{StaticResource DarkScrollViewer}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Page Header -->
            <StackPanel>
                <TextBlock Text="Defender" FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Real-time protection activity log" FontSize="13"
                           Foreground="{StaticResource TextMuted}" Margin="0 4 0 0"/>
                <Border Height="1" Background="{StaticResource BorderDefault}" Margin="0 16"/>
            </StackPanel>

            <!-- Filter pills -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 8">
                <Border Background="{StaticResource BgBadgeGreen}" CornerRadius="12" Padding="10 4" Margin="0 0 8 0">
                    <TextBlock Text="All" FontSize="11" Foreground="{StaticResource TextBadgeGreen}"/>
                </Border>
                <Border Background="{StaticResource BgBadgeRed}" CornerRadius="12" Padding="10 4" Margin="0 0 8 0">
                    <TextBlock Text="Malicious" FontSize="11" Foreground="{StaticResource TextBadgeRed}"/>
                </Border>
                <Border Background="{StaticResource BgBadgeGold}" CornerRadius="12" Padding="10 4" Margin="0 0 8 0">
                    <TextBlock Text="Suspicious" FontSize="11" Foreground="{StaticResource TextBadgeGold}"/>
                </Border>
                <Border Background="{StaticResource BgBadgeGray}" CornerRadius="12" Padding="10 4" Margin="0 0 8 0">
                    <TextBlock Text="Clean" FontSize="11" Foreground="{StaticResource TextBadgeGray}"/>
                </Border>
            </StackPanel>

            <!-- Activity DataGrid -->
            <DataGrid Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False"
                      Background="{StaticResource BgPrimary}" BorderBrush="{StaticResource BorderDefault}"
                      BorderThickness="1" Foreground="{StaticResource TextPrimary}"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      HorizontalGridLinesBrush="{StaticResource BorderDefault}"
                      CanUserResizeRows="False" CanUserSortColumns="True"
                      SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderThickness" Value="3 0 0 0"/>
                                <Setter Property="BorderBrush" Value="{StaticResource AccentGold}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource BgSurface}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Padding" Value="12 8"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderDefault}"/>
                        <Setter Property="BorderThickness" Value="0 0 1 1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TIME" Binding="{Binding Time}" Width="120"/>
                    <DataGridTextColumn Header="TYPE" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="EVENT" Binding="{Binding Event}" Width="*"/>
                    <DataGridTextColumn Header="THREAT LEVEL" Binding="{Binding ThreatLevel}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </ScrollViewer>
</UserControl>
