<UserControl x:Class="Argus.GUI.Views.QuarantineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Argus.GUI.ViewModels">

    <ScrollViewer Style="{StaticResource DarkScrollViewer}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Page Header -->
            <StackPanel>
                <TextBlock Text="Quarantine" FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock FontSize="13" Foreground="{StaticResource TextMuted}" Margin="0 4 0 0">
                    <Run Text="Isolated files awaiting review - "/>
                    <Run Text="{Binding ItemCount}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold"/>
                    <Run Text=" files"/>
                </TextBlock>
                <Border Height="1" Background="{StaticResource BorderDefault}" Margin="0 16"/>
            </StackPanel>

            <!-- Empty state -->
            <Border Grid.Row="1" Padding="16" Background="{StaticResource BgSurface}"
                    CornerRadius="8" Margin="0 0 0 16" BorderBrush="{StaticResource BorderDefault}"
                    BorderThickness="1">
                <TextBlock Text="{Binding StatusMessage}" FontSize="12"
                           Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
            </Border>

            <!-- DataGrid -->
            <DataGrid Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False"
                      Background="{StaticResource BgPrimary}" BorderBrush="{StaticResource BorderDefault}"
                      BorderThickness="1" Foreground="{StaticResource TextPrimary}"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      HorizontalGridLinesBrush="{StaticResource BorderDefault}"
                      CanUserResizeRows="False" CanUserSortColumns="True"
                      SelectionMode="Extended" SelectionUnit="FullRow">
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderThickness" Value="3 0 0 0"/>
                                <Setter Property="BorderBrush" Value="{StaticResource AccentGold}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource BgSurface}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Padding" Value="12 8"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderDefault}"/>
                        <Setter Property="BorderThickness" Value="0 0 1 1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="FILE" Binding="{Binding FileName}" Width="*"/>
                    <DataGridTextColumn Header="HASH" Binding="{Binding Hash}" Width="200"/>
                    <DataGridTextColumn Header="DETECTION" Binding="{Binding DetectionReason}" Width="150"/>
                    <DataGridTextColumn Header="TIMESTAMP" Binding="{Binding Timestamp}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Bottom actions -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0 16 0 0" HorizontalAlignment="Right">
                <Button Content="Release Selected" Style="{StaticResource SecondaryButton}" Margin="0 0 8 0"/>
                <Button Content="Delete Permanently" Style="{StaticResource DangerButton}"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
